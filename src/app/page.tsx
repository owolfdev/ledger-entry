import LedgerInterface from "@/src/components/ledger-interface";

export default function Home() {
  return (
    <main className="h-screen w-full overflow-hidden">
      <LedgerInterface />
    </main>
  );
}
